using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerShooting : MonoBehaviour {

	/*//Ray pullingRay;
	LineRenderer rayLine;
	//GameObject resource;
	bool canPool;
	Vector3 resV;
	//Vector3 mouseP;


	// Use this for initialization
	void Awake () {
		rayLine = GetComponent<LineRenderer>();
		//resource = GameObject.FindGameObjectWithTag ("Resource");
		//resV = resource.transform.position;
	}
	
	// Update is called once per frame
	void Update () {
		bool pool = Input.GetMouseButton (0);
		mouseP = Input.mousePosition;


		if ((pool == true) && (mouseP == resV)) {

			canPool = true;
		}
		Debug.Log ("canpool " + canPool);
		Debug.Log (resource.name);
		Debug.Log ("pool " + pool);
		Debug.Log (mouseP);
		Debug.Log (resV);


	}

	void Puller(){

		if (canPool == true) {
			pullingRay.origin = transform.position;
			pullingRay.direction = resource.transform.position;
			rayLine.enabled = true;
		
		}

	}

*/
}
